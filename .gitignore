__pycache__/
*.pyc
data/*
!data/.gitkeep
!data/events.json
.venv/
venv/
.env
.pytest_cache/

# serenity-aleabitoreddit is installed locally via the `skills` CLI, never
# vendored: the upstream repo declares license: null, so redistributing it
# would be unlicensed. Re-install with:
#   npx -y skills add yan-labs/serenity-aleabitoreddit -a universal --copy -y
.agents/skills/serenity-aleabitoreddit/
.playwright-mcp/

# dev verification screenshots at repo root
/dashboard-final.png
/dashboard-final-check.png
/regime-card-check.png
/verify-risk-card.png

# slim skills runtime state (codemap.json, deepwork/, etc.)
.slim/

# Playwright test-runner junction (global @playwright/test, dev-only)
node_modules/

# Playwright test output
test-results/