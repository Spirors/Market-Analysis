# Vault-local runtime and recovery state may contain note text or source URLs.
.vault-meta/

# Local MCP launch configuration may contain commands or credentials. Share a
# reviewed `.mcp.example.json` instead of committing the live file.
.mcp.json

# Obsidian and operating-system session state.
.obsidian/workspace*.json
.obsidian/graph*.json
.trash/
.DS_Store
Thumbs.db

# Obsidian Volatile Files
.obsidian/cache/

# AI Tooling
.claude-obsidian.json

# Optional: Ignore plugin binaries but keep the list
.obsidian/plugins/
!.obsidian/community-plugins.json

# Uncomment the next line if .raw contains temporary/generated files
.raw/